# 邮箱验证流程方案

## 1. 概述

本文档描述使用 Resend 服务实现邮箱验证码注册/登录验证的完整方案。

### 1.1 验证场景

| 场景 | 说明 |
|------|------|
| 邮箱注册 | 注册时发送验证码到邮箱，验证后完成注册 |
| 邮箱登录 | 支持"验证码登录"模式，无需密码 |
| 忘记密码 | 通过邮箱验证码重置密码 |

### 1.2 技术选型

- **邮件服务商**: Resend (已申请 API Key)
- **验证码存储**: Redis (设置过期时间)
- **验证码格式**: 6位数字

---

## 2. 配置项

在 `.env` 文件中新增以下配置：

```env
# ── Resend 邮件配置 ──────────────────────────────────────────────────
RESEND_API_KEY=re_xxxxxxxxxxxxx
RESEND_FROM_EMAIL=TextLens <noreply@textlens.com>
```

在 `app/config.py` 中新增配置类：

```python
# ── Resend 邮件配置 ──────────────────────────────────────────────────
RESEND_API_KEY: Optional[str] = None
RESEND_FROM_EMAIL: str = "TextLens <noreply@textlens.com>"
```

---

## 3. 数据库模型

### 3.1 新增 Redis Key 设计

| Key 格式 | 说明 | 过期时间 |
|----------|------|----------|
| `email_verify:{email}` | 注册验证码 | 10分钟 |
| `email_login:{email}` | 登录验证码 | 5分钟 |
| `email_reset:{email}` | 密码重置验证码 | 15分钟 |
| `email_code_count:{email}` | 发送次数计数器 | 24小时 |

### 3.2 验证码数据格式

```json
{
  "code": "123456",
  "created_at": 1712812000,
  "attempts": 0
}
```

---

## 4. API 接口设计

### 4.1 发送验证码

```
POST /api/v1/auth/verify/send
```

**请求体**:
```json
{
  "email": "user@example.com",
  "scene": "register" | "login" | "reset_password"
}
```

**响应** (200 OK):
```json
{
  "message": "验证码已发送至邮箱",
  "expires_in": 600
}
```

**业务规则**:
- 同一邮箱同一场景：60秒内不能重复发送
- 同一邮箱每天最多发送 **10 次**
- 注册场景：邮箱已注册返回错误
- 登录/重置场景：邮箱未注册返回错误

---

### 4.2 验证验证码

```
POST /api/v1/auth/verify/check
```

**请求体**:
```json
{
  "email": "user@example.com",
  "scene": "register" | "login" | "reset_password",
  "code": "123456"
}
```

**响应** (200 OK):
```json
{
  "valid": true,
  "token": "eyJhbGciOiJIUzI1NiIs..."
}
```

**业务规则**:
- 验证码错误剩余 **3 次**，错误5次后验证码失效
- 验证成功后，验证码立即失效（一次性）
- 注册验证成功：返回临时 Token 用于完成注册流程
- 登录验证成功：返回正式 JWT Token
- 重置密码验证成功：返回临时 Token 用于重置密码

---

### 4.3 注册接口变更

现有注册接口 `/api/v1/auth/register` 拆分为两步：

**第一步**: 发送验证码（见 4.1）

**第二步**: 完成注册（需携带 verify_token）

```
POST /api/v1/auth/register/complete
```

**请求体**:
```json
{
  "verify_token": "eyJhbGciOiJIUzI1NiIs...",
  "password": "12345678",
  "username": "用户名",
  "age_verified": true,
  "terms_accepted": true,
  "privacy_accepted": true,
  "invite_code": "ABC123"
}
```

---

### 4.4 验证码登录

替代传统密码登录：

```
POST /api/v1/auth/login/code
```

**请求体**:
```json
{
  "email": "user@example.com"
}
```

**响应**: 发送验证码后返回

```
POST /api/v1/auth/login/verify
```

**请求体**:
```json
{
  "email": "user@example.com",
  "code": "123456"
}
```

**响应**:
```json
{
  "access_token": "eyJhbG...",
  "refresh_token": "eyJhbG...",
  "token_type": "bearer"
}
```

---

### 4.5 忘记密码接口

```
POST /api/v1/auth/password/reset/send
```

**请求体**:
```json
{
  "email": "user@example.com"
}
```

```
POST /api/v1/auth/password/reset/confirm
```

**请求体**:
```json
{
  "verify_token": "eyJhbGciOiJIUzI1NiIs...",
  "new_password": "12345678"
}
```

---

## 5. Resend 集成

### 5.1 邮件服务类

新建 `app/external/resend_client.py`:

```python
"""
Resend 邮件发送客户端
封装 Resend API 实现邮件发送功能
"""

import resend
from app.config import settings
from app.core.exceptions import ExternalServiceError

class ResendClient:
    """
    Resend 邮件发送客户端

    [api_key] Resend API Key
    [from_email] 发件人邮箱地址
    """

    def __init__(self):
        self.api_key = settings.RESEND_API_KEY
        self.from_email = settings.RESEND_FROM_EMAIL
        if self.api_key:
            resend.api_key = self.api_key

    def send_verification_email(self, email: str, code: str, scene: str) -> bool:
        """
        发送邮箱验证码邮件

        [email] 收件人邮箱
        [code] 6位验证码
        [scene] 场景 (register/login/reset_password)
        返回 bool 是否发送成功
        """
        subject_map = {
            "register": "TextLens 注册验证码",
            "login": "TextLens 登录验证码",
            "reset_password": "TextLens 密码重置验证码",
        }

        html_content = f"""
        <div style="font-family: sans-serif; max-width: 480px; margin: 0 auto;">
            <h2>TextLens</h2>
            <p>您好，</p>
            <p>您的验证码是：<strong style="font-size: 24px; letter-spacing: 4px;">{code}</strong></p>
            <p>该验证码在 <strong>10 分钟</strong>内有效，请勿泄露给他人。</p>
            <hr style="border: none; border-top: 1px solid #eee; margin: 20px 0;">
            <p style="color: #666; font-size: 12px;">如果您没有发起此请求，请忽略此邮件。</p>
        </div>
        """

        try:
            if not self.api_key:
                # 开发环境：打印到日志
                print(f"[Dev Mode] Email to {email}, code: {code}, scene: {scene}")
                return True

            response = resend.Emails.send({
                "from": self.from_email,
                "to": email,
                "subject": subject_map.get(scene, "TextLens 验证码"),
                "html": html_content,
            })
            return response.get("id") is not None
        except Exception as e:
            raise ExternalServiceError(f"Failed to send email: {str(e)}")

resend_client = ResendClient()
```

### 5.2 验证码服务

新建 `app/features/auth/verification_service.py`:

```python
"""
邮箱验证码服务
处理验证码的生成、存储、校验逻辑
"""

import random
import hashlib
import json
from datetime import datetime, timedelta, timezone
from typing import Optional, Tuple
from app.core.exceptions import ValidationError, RateLimitError
from app.config import settings
import redis

class VerificationService:
    """
    邮箱验证码服务

    负责验证码生命周期管理：生成、发送、校验、限流
    """

    # 验证码长度
    CODE_LENGTH = 6

    # 各场景过期时间（秒）
    EXPIRY_SECONDS = {
        "register": 600,      # 10分钟
        "login": 300,          # 5分钟
        "reset_password": 900, # 15分钟
    }

    # 发送频率限制（秒）
    RATE_LIMIT_SECONDS = 60

    # 每日最大发送次数
    DAILY_MAX_SENDS = 10

    # 最大校验失败次数
    MAX_ATTEMPTS = 5

    def __init__(self, redis_client: redis.Redis):
        self.redis = redis_client

    def _get_key(self, email: str, scene: str) -> str:
        """获取 Redis Key"""
        return f"email_verify:{scene}:{email}"

    def _get_count_key(self, email: str) -> str:
        """获取计数器 Key"""
        return f"email_code_count:{email}"

    def _generate_code(self) -> str:
        """
        生成6位数字验证码

        返回 str 6位数字字符串
        """
        return str(random.randint(100000, 999999))

    def _hash_code(self, code: str) -> str:
        """对验证码进行哈希（存储时脱敏）"""
        return hashlib.sha256(code.encode()).hexdigest()

    def _is_rate_limited(self, email: str, scene: str) -> bool:
        """检查是否在频率限制期内"""
        key = f"email_rate:{scene}:{email}"
        return self.redis.exists(key)

    def _set_rate_limit(self, email: str, scene: str) -> None:
        """设置频率限制"""
        key = f"email_rate:{scene}:{email}"
        self.redis.setex(key, self.RATE_LIMIT_SECONDS, "1")

    def _check_daily_limit(self, email: str) -> bool:
        """检查是否超过每日发送次数"""
        key = self._get_count_key(email)
        count = self.redis.get(key)
        if count is None:
            return False
        return int(count) >= self.DAILY_MAX_SENDS

    def _increment_daily_count(self, email: str) -> int:
        """增加每日发送计数"""
        key = self._get_count_key(email)
        pipe = self.redis.pipeline()
        pipe.incr(key)
        pipe.expire(key, 86400)  # 24小时过期
        results = pipe.execute()
        return results[0]

    async def send_code(self, email: str, scene: str) -> Tuple[bool, int]:
        """
        发送验证码

        [email] 目标邮箱
        [scene] 场景 register/login/reset_password
        返回 (是否成功, 剩余有效期秒数)
        """
        # 频率限制检查
        if self._is_rate_limited(email, scene):
            raise RateLimitError(f"发送过于频繁，请 {self.RATE_LIMIT_SECONDS} 秒后重试")

        # 每日次数检查
        if self._check_daily_limit(email):
            raise RateLimitError("今日发送次数已用完，请明天再试")

        # 生成验证码
        code = self._generate_code()
        expiry = self.EXPIRY_SECONDS.get(scene, 600)

        # 存储验证码（Redis）
        key = self._get_key(email, scene)
        data = json.dumps({
            "code": self._hash_code(code),
            "created_at": int(datetime.now(timezone.utc).timestamp()),
            "attempts": 0,
        })
        self.redis.setex(key, expiry, data)

        # 发送邮件（省略 Resend 调用）
        # await resend_client.send_verification_email(email, code, scene)

        # 设置频率限制
        self._set_rate_limit(email, scene)

        # 增加每日计数
        self._increment_daily_count(email)

        return True, expiry

    async def verify_code(self, email: str, scene: str, code: str) -> bool:
        """
        校验验证码

        [email] 目标邮箱
        [scene] 场景
        [code] 用户输入的验证码
        返回 bool 是否验证通过
        """
        key = self._get_key(email, scene)
        data = self.redis.get(key)

        if not data:
            raise ValidationError("验证码已过期，请重新发送")

        info = json.loads(data)

        # 校验失败次数超限
        if info["attempts"] >= self.MAX_ATTEMPTS:
            self.redis.delete(key)
            raise ValidationError("验证失败次数过多，请重新发送验证码")

        # 验证码匹配
        if info["code"] == self._hash_code(code):
            # 验证成功，删除验证码（一次性）
            self.redis.delete(key)
            return True

        # 匹配失败，增加失败次数
        info["attempts"] += 1
        self.redis.setex(key, self.EXPIRY_SECONDS.get(scene, 600), json.dumps(info))

        remaining = self.MAX_ATTEMPTS - info["attempts"]
        raise ValidationError(f"验证码错误，剩余 {remaining} 次尝试机会")

    def create_verify_token(self, email: str, scene: str) -> str:
        """
        创建验证通过后的临时 Token

        [email] 已验证的邮箱
        [scene] 验证场景
        返回 str JWT Token
        """
        from app.core.security import create_access_token

        payload = {
            "sub": email,
            "scene": scene,
            "verified": True,
        }
        return create_access_token(payload, expires_delta=timedelta(minutes=15))

    def verify_token(self, token: str, expected_scene: str) -> str:
        """
        验证临时 Token，获取邮箱

        [token] 临时 Token
        [expected_scene] 期望的场景
        返回 str 邮箱地址
        """
        from app.core.security import verify_access_token
        from app.core.exceptions import AuthenticationError

        payload = verify_access_token(token)

        if not payload.get("verified"):
            raise AuthenticationError("未完成邮箱验证")

        if payload.get("scene") != expected_scene:
            raise AuthenticationError("验证场景不匹配")

        return payload.get("sub")
```

---

## 6. 业务流程

### 6.1 邮箱注册流程

```
┌─────────────┐      ┌─────────────────┐      ┌─────────────────┐
│   用户      │      │   发送验证码     │      │   完成注册       │
│  ───────►  │ ───► │  POST /send     │ ───► │ POST /complete  │
└─────────────┘      └─────────────────┘      └─────────────────┘
                           │                        │
                           ▼                        ▼
                    ┌─────────────┐          ┌─────────────┐
                    │ Resend API  │          │ 创建用户    │
                    │ 发送邮件    │          │ 发放积分   │
                    └─────────────┘          └─────────────┘
```

### 6.2 验证码登录流程

```
┌─────────────┐      ┌─────────────────┐      ┌─────────────────┐
│   用户      │      │   请求登录验证码  │      │   验证登录       │
│  ───────►  │ ───► │  POST /login    │ ───► │ POST /verify    │
└─────────────┘      └─────────────────┘      └─────────────────┘
                           │                        │
                           ▼                        ▼
                    ┌─────────────┐          ┌─────────────┐
                    │ Resend API  │          │ 返回 JWT    │
                    │ 发送邮件    │          │ 登录成功    │
                    └─────────────┘          └─────────────┘
```

### 6.3 忘记密码流程

```
┌─────────────┐      ┌─────────────────┐      ┌─────────────────┐
│   用户      │      │   请求重置邮件   │      │   确认新密码     │
│  ───────►  │ ───► │  POST /reset    │ ───► │ POST /confirm  │
└─────────────┘      └─────────────────┘      └─────────────────┘
                           │                        │
                           ▼                        ▼
                    ┌─────────────┐          ┌─────────────┐
                    │ Resend API  │          │ 更新密码    │
                    │ 发送邮件    │          │ 登录成功    │
                    └─────────────┘          └─────────────┘
```

---

## 7. 安全性设计

| 措施 | 说明 |
|------|------|
| 验证码哈希存储 | 存储 SHA-256 哈希值，不存储明文验证码 |
| 一次性使用 | 验证成功后立即删除验证码 |
| 失败次数限制 | 错误5次后验证码失效 |
| 频率限制 | 同一邮箱同一场景60秒内不能重复发送 |
| 每日次数限制 | 同一邮箱每天最多发送10次 |
| Token 时效 | 验证通过后的临时 Token 仅15分钟有效 |
| HTTPS | 生产环境强制 HTTPS 传输 |

---

## 8. 错误码对照表

| 错误信息 | HTTP 状态码 | 说明 |
|----------|-------------|------|
| 验证码已过期 | 400 | 超过10分钟未验证 |
| 验证码错误，剩余 X 次 | 400 | 输入错误，可重试 |
| 验证失败次数过多 | 400 | 错误5次，需重新发送 |
| 发送过于频繁 | 429 | 60秒内重复发送 |
| 今日发送次数已用完 | 429 | 超过每日10次限制 |
| 邮箱已注册 | 400 | 注册场景邮箱重复 |
| 邮箱未注册 | 400 | 登录/重置场景邮箱不存在 |

---

## 9. 实施计划

### Phase 1: 基础设施 ✅
- [x] 配置 Resend API Key
- [x] 实现 ResendClient 邮件发送客户端
- [x] 实现 VerificationService 验证码服务

### Phase 2: 注册流程 ✅
- [x] 新增 `/auth/verify/send` 接口
- [x] 新增 `/auth/verify/check` 接口
- [x] 修改 `/auth/register/complete` 接口

### Phase 3: 登录流程 ✅
- [x] 新增 `/auth/login/code` 接口
- [x] 新增 `/auth/login/verify` 接口
- [ ] 登录页面增加"验证码登录"选项（前端任务）

### Phase 4: 密码重置 ✅
- [x] 新增 `/auth/password/reset/send` 接口
- [x] 新增 `/auth/password/reset/confirm` 接口

### Phase 5: 测试与文档
- [ ] 单元测试覆盖
- [ ] 集成测试
- [ ] 更新 API 文档

---

## 10. 已创建/修改的文件清单

| 文件 | 操作 | 说明 |
|------|------|------|
| `.env` | 修改 | 添加 RESEND_API_KEY 和 RESEND_FROM_EMAIL |
| `app/config.py` | 修改 | 添加 Resend 相关配置项 |
| `app/external/resend_client.py` | 新建 | Resend 邮件发送客户端 |
| `app/features/auth/verification_service.py` | 新建 | 验证码服务（生成、存储、校验） |
| `app/schemas/user.py` | 修改 | 添加新的请求/响应模型 |
| `app/features/auth/router.py` | 修改 | 添加新的 API 接口 |
| `app/features/auth/service.py` | 修改 | 添加验证码相关业务逻辑 |
| `app/core/exceptions.py` | 修改 | RateLimitError 支持自定义消息 |
| `docs/邮箱验证流程方案.md` | 新建 | 完整方案文档 |
